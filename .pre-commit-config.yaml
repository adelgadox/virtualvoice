repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Detect secrets (gitleaks)
        language: system
        entry: gitleaks protect --staged --redact
        pass_filenames: false
